// PlaceholderRoom is no longer used — scene objects are driven by the store via SceneObject.
// Kept as an empty module to avoid broken imports if referenced elsewhere.
export default function PlaceholderRoom() {
  return null;
}
